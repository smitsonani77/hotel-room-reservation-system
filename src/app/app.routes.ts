import { Routes } from '@angular/router';
import { HotelReservationComponentComponent } from './components/hotel-reservation-component/hotel-reservation-component.component';

export const routes: Routes = [
    {
        path: '',
        component: HotelReservationComponentComponent,
    }
];
